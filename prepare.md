# Prepare — wish-pool 決策記錄

> **版本**：WP-006 · 2026-08-03
> 記錄規則繼承根 `prepare.md`，此處只記本專案落地細節。
> 主線決策見根目錄 `prepare.md`（← D-005）。

---

## 決策日誌（新的在上）

### WP-006 · 2026-08-03 · product
- **決策**：🧪 願意幫測 點擊時就地展開**選填 email**，存進 Sheet 的 `testers` 分頁；
  反饋表單**依角色分兩套題目**（`?r=wisher|tester`）；**「開發中」不寄通知**，只在完成時寄
- **理由**：匿名的 🧪 只是一個數字，**聯絡不到人就等於收不到訊號**——這是原設計的缺陷。
  許願者與願測者能回答的問題不同（前者知道痛點解了沒、後者看得到新手障礙），
  混成一張表兩種洞察都會稀釋成「還不錯」
- **棄選**：純匿名計數（拿不到人，訊號無法兌現）；強制填 email（提高摩擦、壓低表態率）；
  單一反饋表單（問不到各自的重點）；開發中寄進度通知（**開發可能拖很久，先寄等於製造期待然後失約**）
- **個資**：願測者 email **同 WP-002 一律不進 Notion**，只存 Sheet；
  Notion 僅放 `願測人數`。每封信必附退訂連結，`testers` 分頁有 `unsubscribed` 欄
- **反饋表單第一題是過濾題**（試用了嗎）——沒試用就不問後面，否則收到的是憑印象的空話
- **落地**：README P7 新增三個子節；實作待 P4 完成

### WP-005 · 2026-08-03 · product
- **決策**：許願列表加投票，採**三維表情反應**（🙋 我也需要／🧪 願意幫測／💡 有趣），
  做在**網站列表頁**而非 Notion；最下方三張聲量圖表（票數排行、狀態分佈、時間趨勢）
- **理由**：訪客不用寫許願也能表態，直接解掉冷啟動（待討論 #3）；
  `願意幫測` 是最稀有的承諾訊號，可直接餵給反饋迴圈
- **棄選**：每人 3 票（強迫排序、排名較實，但用戶選擇多維度）；
  無限 +1（測的是人脈不是需求）；做在 Notion（**技術上不可行**，不支援匿名訪客互動）
- **已知代價**：**三個維度會互相矛盾**（需求高但沒人覺得有趣，或反之），
  提案時已標示此風險，用戶確認採用。因此**必須先定排序規則**，否則圖表反而讓人更難決定
- **排序規則**：主鍵 `我也需要`，同分比 `願意幫測`（承諾 > 興趣）；`有趣` 不進排序
- **落地**：README「開發階段」新增 P7（依賴 P4，需線上站台）；實作待 P4 完成

### WP-004 · 2026-08-03 · process
- **決策**：開發階段規劃寫進 `README.md` 的「開發階段」章節（依 `.claude/templates/README.template.md:70`），
  不另開 `docs/ROADMAP.md`
- **理由**：規劃原本只存在對話裡，session 結束就消失、`/catchup` 也撈不回來。
  模板已經指定過位置，另開檔案是憑印象發明結構
- **棄選**：`docs/ROADMAP.md`（違反模板規定）；繼續只放在對話裡（已證實會遺失）
- **落地**：README 新增「開發階段」章節，含 P0–P6、出口條件、依賴圖、已知未完成清單

### WP-003 · 2026-08-03 · design
- **決策**：本站**固定為夜空，不跟隨系統的 `prefers-color-scheme`**。
  移除所有淺色模式覆寫，深淺色使用者看到同一個場景
- **理由**：星星、流星、仙女棒、水面倒影在白底上全部消失——跟隨系統主題等於讓一半的
  使用者看不到這個作品的主題本身
- **棄選**：跟隨系統主題（主題會消失）；做一套「日間許願池」淺色場景
  （要重做整組光效，成本不成比例）
- **代價**：偏好淺色的使用者拿不到自己的設定。這是刻意接受的
- **落地**：`web/styles.css` 移除五處 `@media (prefers-color-scheme: light)` 覆寫；
  對比度改以夜空底色為基準調校
- **注意**：`prefers-reduced-motion` **仍然完全尊重**——不跟隨主題偏好不等於不跟隨無障礙偏好

### WP-002 · 2026-08-02 · data
- **決策**：email **只存 Google Sheet，不寫進 Notion**。Notion 那頁可公開，列表顯示許願內容與進度但不顯示許願者
- **理由**：收 email 就是收個資；許願列表要公開才有「看得到別人許什麼」的價值，兩者衝突時切開存放而不是二選一
- **棄選**：email 寫進 Notion 但設欄位權限（Notion 的欄位權限做不到「頁面公開但單欄隱藏」）；不公開列表（失去公開的價值）
- **落地**：`Notion.gs` 的 `createNotionWish()` 不組 email 屬性；`analysis/PROMPT.md` 明寫「不要自己補」

### WP-001 · 2026-08-02 · reliability
- **決策**：寫進 Sheet 之後，**寄信與 Notion 同步失敗都不回滾、不拋出**，只記錄到該列的 `notionSyncError`，用 `retryFailedSyncs()` 手動補送
- **理由**：使用者的心力在按下送出時已經付出，第三方 API 打嗝不該讓他重填一次表單
- **棄選**：失敗即整筆失敗（使用者體驗最差）；自動排程重試（同步失敗應是罕見事件，值得看一眼原因，自動重試會把問題藏起來）
- **代價**：會出現「Sheet 有、Notion 沒有」的短暫不一致，這是刻意換的
- **落地**：`Code.gs` 的 `trySendConfirmation()` / `trySyncToNotion()` 都回傳錯誤而非拋出

### WP-000 · 2026-08-02 · architecture
- **決策**：**自建靜態表單頁（GitHub Pages）＋ GAS Web App 後端 ＋ Google Sheet 資料庫 ＋ Notion 展示層**。「sheet 匯出」定位為**資料庫本身即匯出格式**，不另做匯出功能
- **理由**：零主機成本與零 DB 維運；表單外觀可完全客製（作品集賣相優於 Google Form）；一次補上完全沒有作品的第 4 關 GAS
- **棄選**：純 Google Form + Sheet（前端零展示成果）；自建 Web + PostgreSQL + Resend 部署 Zeabur（維運面最廣，且第 6 關已有 knowledge-platform）
- **落地**：`web/` + `gas/` + Notion「🌠 許願列表」資料庫（狀態機九態）
- **已知天花板**：GmailApp 每天 100 封、GAS 單次執行 6 分鐘。目前量級遠低於此

---

## 待討論事項

| # | 議題 | 卡在什麼 | 提出日期 |
|---|------|---------|---------|
| 1 | 反饋迴圈要不要做成第二張表單 | 完成信附反饋表單連結，但那張表單還沒做；先看有沒有人真的許願再決定 | 2026-08-02 |
| 2 | 分析層要不要改成全自動 | 目前半自動（Claude Code 在場）。等分析格式穩定、且真的有量了再評估 API 化 | 2026-08-02 |
| 3 | 冷啟動要不要自己塞真實案例 | 沒人許願時列表是空的，空列表不會吸引人許願 | 2026-08-02 |
| 4 | 視覺調整的出口條件 | 色調已改十二版仍未收斂。沒有出口條件會無限調下去，且每次提亮都在跟可讀性拉扯（已洗白畫面兩次）。建議定為「願意把連結貼給朋友就停」，待確認 | 2026-08-03 |
| 5 | 第一批許願者從哪來 | **這是整個專案真正的風險**——不是技術。沒有人來許願，這個作品就退回成「自己想的題目」，失去它存在的理由 | 2026-08-03 |
